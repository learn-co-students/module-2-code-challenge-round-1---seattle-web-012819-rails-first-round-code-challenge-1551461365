<% if flash[:notice] %>
<%= flash[:notice] %>
<% end %>

<h1>Make a New Heroine!</h1>
<%= form_for @heroine do |f| %>
 <h3> <%= f.label :name %></h3>
  <%= f.text_field :name %>
  <h3><%= f.label :super_name %></h3>
  <%= f.text_field :super_name %>
   <h3><%= f.collection_select :power_id, @powers, :id, :name%></h3>
   <% 
   #would titleize options in select options with more time
   %>
  <%= f.submit %>
<% end %>